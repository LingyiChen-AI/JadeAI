# JadeAI 出客户端了：不用装 Docker、不用配数据库，双击就能写简历（macOS / Windows）

> GitHub: https://github.com/twwch/JadeAI ｜ 官网：https://jadeai.cturing.cn/zh
> 下载：https://github.com/twwch/JadeAI/releases/latest

大家好。JadeAI 开源大半年，收到最多的一类反馈，大概可以概括成一句话：

> "东西挺好，但我就想改个简历，为什么要先学会 Docker？"

这话说得没错。之前想用 JadeAI，路径是这样的：装 Docker → 生成一个 `AUTH_SECRET` → 记住那行 `docker run` → 挂个卷别让数据丢了 → 打开 `localhost:3000`。对开发者来说这不算什么，但简历这东西，产品经理要写、设计师要写、应届生也要写。为了改两行工作经历去装一个容器运行时，属实有点离谱。

所以这次做了个**桌面客户端**。macOS 和 Windows 都有，下载装上,双击打开就能用。

---

## 先说结论

它跑的是和网页版**完全相同**的那个应用。50 套模板、拖拽编辑、AI 对话优化、语法检查、JD 匹配分析、职业照生成、模拟面试、多格式导出——**一个都没少**，不是什么"精简版"。

区别只有一个：**服务跑在你自己的电脑上**。

具体带来这几点变化：

- **零配置** —— 不需要 Docker、不需要数据库、不需要 `AUTH_SECRET`，装完打开就是能用的状态
- **不需要账号** —— 本机单用户，没有注册、没有登录、没有浏览器指纹识别
- **数据在你自己硬盘上** —— 简历存在本机一个 SQLite 文件里，不经过任何服务器，包括我的
- **AI 还是用你自己的 Key** —— 在应用内「设置 → AI」里填，请求直连你配置的服务商，中间不过我的手
- **会自己提示更新** —— 有新版本时右下角冒一个小条，点一下就把对应你机器的安装包下好

有一点我得说清楚，不然上面那句"数据不出本机"就是在吹牛：**它启动时会向 GitHub 问一次有没有新版本**，这是它唯一的对外网络请求。介意的话可以关掉，方法在文末。

---

## 下载

去 **[Releases 页面](https://github.com/twwch/JadeAI/releases/latest)** 挑对应你系统的那个包：

| 你的电脑 | 下载哪个 |
|---|---|
| **Mac**（M1/M2/M3/M4 等 Apple 芯片） | `JadeAI-*-mac-arm64.dmg` |
| **Mac**（2020 年前后的 Intel 芯片） | `JadeAI-*-mac-x64.dmg` |
| **Windows**（64 位） | `JadeAI-*-win-x64-setup.exe` |

> 不确定自己的 Mac 是哪种芯片？点左上角  → 「关于本机」，芯片那一栏写 **Apple M 系列**就选 arm64，写 **Intel** 就选 x64。选错了装不上，但不会有别的损失，换一个重下就行。

---

## 安装：第一次打开会被系统拦一下

**这一步请务必看完，不然你大概率会以为软件坏了。**

安装包带的是 **ad-hoc 签名**——签名是有效的，但它是匿名的，没有花钱买 Apple 的开发者证书（$99/年），也没有做公证。所以 macOS 和 Windows 第一次都会拦一下。这是系统的默认策略，不是软件有问题。

### macOS

把 `JadeAI.app` 拖进「应用程序」之后，**先在终端跑这一条**：

```bash
xattr -dr com.apple.quarantine /Applications/JadeAI.app
```

然后正常双击打开就行了。这条命令做的事是：把"这个文件是从网上下载的"这个标记去掉。

不想用终端的话，也可以走图形界面：双击 → 系统拦下来 → 点「完成」→ 打开**系统设置 → 隐私与安全性** → 拉到最底下会出现一个「仍要打开」按钮 → 点它 → 再确认一次。

> ⚠️ **别去试"右键 → 打开"**。这个老办法在 **macOS 15 之后已经被苹果移除了**，对未公证的应用不再生效。网上很多教程还在教这个，照做只会浪费时间。

**另外分清楚两种提示**，它们完全不是一回事：

| 系统提示 | 什么意思 | 怎么办 |
|---|---|---|
| "**无法验证**开发者" | 正常。签名有效，只是没公证 | 按上面的步骤放行 |
| "JadeAI **已损坏**，无法打开" | 文件不完整或被改动过 | **重新下载**，别放行 |

### Windows

双击 `.exe` 后 SmartScreen 会弹窗，点「**更多信息**」→「**仍要运行**」。然后按安装向导走，可以自己选安装目录，是**当前用户安装**，不需要管理员权限。

---

## 你的简历存在哪

| 系统 | 路径 |
|---|---|
| macOS | `~/Library/Application Support/JadeAI/` |
| Windows | `%APPDATA%\JadeAI\` |

目录里 `jade.db` 是数据库（所有简历都在里面），`jade-settings.json` 是窗口大小、语言之类的偏好。

**卸载应用不会删除这个目录**——这是故意的，免得手滑卸载把简历一起弄没了。真要清空数据，手动删掉这个文件夹即可。

想备份简历？把 `jade.db` 拷走就是完整备份。换电脑的话，装好新的客户端、把这个文件拷回去，简历就都在了。

---

## 更新怎么走

启动时客户端会问一次 GitHub 有没有新版本。有的话，窗口**右下角**会出现一个小提示条——不是那种糊你一脸、不点就没法继续的弹窗，你可以完全无视它继续写简历。

点「立即下载」，它会**自动挑对应你这台机器架构的安装包**下载（不用你在三个文件里对着猜），进度就显示在那个小条里。下完给你两个按钮：「打开安装包」和「在文件夹中显示」。

安装还是得你自己点一下（Mac 拖进「应用程序」，Windows 跑一下 exe）。这一步没法自动化：静默安装需要走苹果的 Squirrel 机制，而它不接受 ad-hoc 签名的应用。等哪天买了开发者证书再说。

**更新不会动你的数据。**覆盖安装就行，简历原样都在。

不想让它联网查更新的话，打开上面那个数据目录里的 `jade-settings.json`，把

```json
"updateCheckEnabled": false
```

改进去，重启客户端即可。改完它就是一个彻底不联网的本地应用了（AI 功能除外，那个是直连你自己配的服务商）。

---

## 网页版还在，而且没变

客户端不是来取代网页版的，两边是**同一份代码**，同一个版本号，同一次发布一起打出来。

需要多人共用、需要挂在服务器上、需要用 PostgreSQL、需要给团队开账号——那还是用 Docker 部署网页版：

```bash
# 先生成一个密钥
openssl rand -base64 32

docker run -d -p 3000:3000 \
  -e AUTH_SECRET=<你生成的密钥> \
  -v jadeai-data:/app/data \
  twwch/jadeai:latest
```

**自己一个人写简历**，那就用客户端，省事。

---

## 顺手修的一个 bug

这一版还修了个让我印象挺深的问题：有用户反馈**导出功能用着用着就坏了，重启服务又好了，过一阵子再坏**。

查下来根因挺有意思：每次导出 PDF 都会起一个 Chromium，代码里是正常关闭的——那些进程确实死了。但它们死的时候，Chromium 的辅助进程被挂到了容器的 1 号进程下，而 1 号进程是 Node，**Node 不会去回收自己没生过的子进程**。于是每导出一次，就留下几个没人收尸的僵尸进程。

僵尸进程**不占内存**——这就是它显得特别莫名其妙的原因：容器看着一切正常，内存曲线平得很，日志里什么都没有。直到进程表被填满，Chromium 再也起不来，从那一刻起所有导出全部失败，重启清空进程表又活了，周而复始。

实测 5 次导出留下 20 个僵尸，内存全程稳定在 346MB。修法是往镜像里加了个 `tini` 当 1 号进程负责回收，重新构建后连续 8 次导出，僵尸数 0。

用 Docker 部署的朋友 `docker pull twwch/jadeai:latest` 更新一下就好了。

---

## 写在最后

项目完全开源，Apache 2.0 协议。客户端和网页版都免费，没有任何功能是收费解锁的。

**GitHub: https://github.com/twwch/JadeAI**
**下载: https://github.com/twwch/JadeAI/releases/latest**

如果觉得有用，欢迎点个 Star。用下来有问题或者有想要的功能，欢迎提 Issue，也欢迎 PR。

最后再啰嗦一次那条最容易卡住人的命令，Mac 用户第一次打开前跑它：

```bash
xattr -dr com.apple.quarantine /Applications/JadeAI.app
```
